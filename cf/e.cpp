#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    while(n--)
    {
        int t;
        cin>>t;
        set<int>a;
        for(int i=0;i<t;i++)
        {
            int tmp;
            cin>>tmp;
            a.insert(tmp);
        }
        for(int i<0;i<t;i++)
        {
            int now=a.count(i);

        }

    }
}
